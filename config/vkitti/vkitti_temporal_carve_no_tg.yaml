# @package _global_
# CARVE objective ablation L2: remove only temporal gradient matching.
# Compare against vkitti_temporal_carve_full (alpha=0.5, stable=10).
defaults:
  - vkitti_temporal_carve_full
  - _self_

training:
  checkpoint_dir: ./checkpoint/vkitti_temporal_carve_no_tg
  log_dir: ./logs/vkitti_temporal_carve_no_tg

loss:
  objective: video
  kwargs:
    alpha: 0.5
    stable_scale: 0.0

project_name: vkitti_temporal_carve_no_tg
