training:
  seed: 0
  checkpoint_dir: ./checkpoint/gemdepth_stage2
  save_freq: 5000
  val_freq: 10000
  log_dir: ./logs/stage2

optimizer:
  kind: adamw
  learning_rate: 1.0e-4
  dec_lr: 1.0e-5
  other_lr: 1.0e-6
  weight_decay: 0.01

dataset:
  train:
    mode: train
    data_dirs:  ['']
    crop_size: 518
    seq_len: 32
  
  val:
    mode: train
    data_dirs: []
    crop_size: 518
    seq_len: 32
    
dataloader:
  batch_size: 8
  num_workers: 4

model:
  video_path: "./checkpoint/gemdepth.pth"

encoder: 'vitl'
pose_flag: True
num_gpus: 8
total_step: 100000
project_name: video
resume: false  # set to path or true for auto-resume

